/**
 * Web twin: the browser build ships no catalogue, so anything computed from it
 * on the device — the heat maps, the Explore grids — cannot run here.
 * See connection.web.js for why.
 */
export const CATALOGUE_AVAILABLE = false;
